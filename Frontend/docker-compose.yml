services:
  frontend:
    build:
      context: .
      dockerfile: Dockerfile
    ports:
      - "4200:4200"
    volumes:
      - ./certs/localhost.crt:/app/certs/localhost.crt:ro
      - ./certs/localhost.key:/app/certs/localhost.key:ro
    restart: unless-stopped
